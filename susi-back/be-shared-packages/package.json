{
  "name": "geobuk-shared",
  "version": "1.0.0",
  "private": true,
  "description": "거북스쿨 마이크로서비스 공유 패키지",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "yarn workspaces run clean",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test",
    "publish:local": "yarn workspaces run publish:local"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
