import { IconChevronDown } from "@tabler/icons-react";
import { Link, useNavigate } from "@tanstack/react-router";
import { Button, buttonVariants } from "./custom/button";
import { GradeBookmarkBar } from "./grade-bookmark-bar";
import { useGradeStore } from "@/stores/client/use-grade-store";
// import headerLogo from "@/assets/icon/header-logo.png";
// import headerLogo2 from "@/assets/icon/header-logo2.png";
import React, { useState } from "react";
import { cn } from "@/lib/utils";
import {
  NavigationMenuItem,
  NavigationMenuList,
  NavigationMenu,
  NavigationMenuContent,
  NavigationMenuLink,
  NavigationMenuTrigger,
  navigationMenuTriggerStyle,
} from "./ui/navigation-menu";
import { useAuthStore } from "@/stores/client/use-auth-store";
import { useQuery } from "@tanstack/react-query";
import { USER_API } from "@/stores/server/features/me/apis";
import { Popover, PopoverContent, PopoverTrigger } from "./ui/popover";
import { Separator } from "./ui/separator";
import { toast } from "sonner";
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "./ui/dialog";
import { useQueryClient } from "@tanstack/react-query";
import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "./ui/sheet";
import { Menu } from "lucide-react";
import { clearTokens as clearTokenManager } from "@/lib/api/token-manager";
import { useTokenStore } from "@/stores/atoms/tokens";

export const Header = () => {
  const queryClient = useQueryClient();
  const navigate = useNavigate();
  const { grade } = useGradeStore();

  // 로그인 페이지에서는 API 호출하지 않음 (무한 루프 방지)
  const isLoginPage = window.location.pathname === "/auth/login";
  const isTestPage = window.location.pathname === "/test/auth-me";
  const isRegisterPage = window.location.pathname === "/auth/register";
  const isResetPasswordPage = window.location.pathname === "/auth/reset-password";

  // 인증 관련 페이지에서는 API 호출하지 않음
  const isAuthPage = isLoginPage || isTestPage || isRegisterPage || isResetPasswordPage;

  // Queries - 인증 페이지에서는 비활성화 (무한 루프 방지)
  const { data: user } = useQuery({
    queryKey: ["me"],
    queryFn: USER_API.fetchCurrentUserAPI,
    enabled: !isAuthPage, // 인증 페이지에서는 비활성화
    retry: false, // 401 에러 시 재시도하지 않음
    staleTime: 5 * 60 * 1000, // 5분간 캐시 유지
  });
  const { data: isOfficer } = useQuery({
    queryKey: ["officer", "check"],
    queryFn: () => USER_API.fetchCurrentUserAPI().then(user => {
      // 간단한 officer 체크 로직 (실제 로직은 복잡할 수 있음)
      return user?.role === "officer" || false;
    }),
    enabled: !isAuthPage && !!user, // 인증 페이지 아니고 user가 있을 때만
    retry: false,
  });

  // Zustand
  const { clearTokens } = useAuthStore();

  const [isOpen, setIsOpen] = useState<boolean>(false);

  const handleLogoutClick = () => {
    // 모든 토큰 저장소 비우기
    clearTokens();                              // useAuthStore (auth-storage)
    clearTokenManager();                        // token-manager (localStorage)
    useTokenStore.getState().clearTokens();     // useTokenStore (token-storage)

    queryClient.clear();
    toast.success("안녕히 가세요 👋");
    navigate({ to: "/" });
  };

  return (
    <>
      <GradeBookmarkBar />
      <header className="sticky top-0 z-40 w-full border-b-[1px] bg-white">
      <div className="mx-auto">
        <div className="container flex h-14 w-screen items-center justify-between lg:h-16">
          {/* 로고 */}
          <Link to="/" className="flex shrink-0 items-center gap-3">
            <img src="/logo.png" alt="logo" className="h-auto w-10 lg:w-12" />
            <div className="text-base font-medium text-primary lg:text-lg">
              거북스쿨
            </div>
          </Link>

          {/* mobile */}
          <span className="flex lg:hidden">
            <Sheet open={isOpen} onOpenChange={setIsOpen}>
              <SheetTrigger className="px-2">
                <Menu
                  className="flex h-5 w-5 lg:hidden"
                  onClick={() => setIsOpen(true)}
                >
                  <span className="sr-only">Menu Icon</span>
                </Menu>
              </SheetTrigger>

              <SheetContent
                side={"left"}
                className="w-[300px] overflow-y-auto sm:w-[400px]"
              >
                <SheetHeader>
                  <SheetTitle className="flex items-center gap-3">
                    <img
                      src="/logo.png"
                      alt="logo"
                      className="h-auto w-10 lg:w-12"
                    />
                    <div className="text-base font-medium text-primary lg:text-lg">
                      거북스쿨
                    </div>
                  </SheetTitle>
                </SheetHeader>
                <nav className="mt-4 flex flex-col items-start justify-center gap-4">
                  <Link
                    to="/"
                    className={cn(
                      buttonVariants({ variant: "ghost" }),
                      "w-full justify-start px-1",
                    )}
                    onClick={() => setIsOpen(false)}
                  >
                    🏠 홈
                  </Link>
                  <div className="w-full space-y-1">
                    <Link
                      to="/planner"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📅 플래너
                    </Link>
                    <Link
                      to="/planner/learning"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📚 학습현황
                    </Link>
                    <Link
                      to="/planner/class"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      👨‍🏫 수업현황
                    </Link>
                  </div>
                  {/* 내신성적분석 - 고3, N수생 제외 */}
                  {grade !== "3" && grade !== "2N" && (
                  <div className="w-full space-y-1">
                    <div className={cn(
                      buttonVariants({ variant: "ghost" }),
                      "w-full justify-start px-1 font-semibold",
                    )}>
                      📊 내신성적분석
                    </div>
                    <Link
                      to="/grade-analysis/school-record"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📄 생기부 입력
                    </Link>
                    <Link
                      to="/grade-analysis/performance"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📊 성적 분석
                    </Link>
                    <Link
                      to="/grade-analysis/request"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      🧑‍🏫 평가 신청
                    </Link>
                    <Link
                      to="/grade-analysis/evaluation-list"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📋 평가 내역
                    </Link>
                    <Link
                      to="/grade-analysis/subject"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📚 교과 전형
                    </Link>
                    <Link
                      to="/grade-analysis/comprehensive"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      🧐 학종 전형
                    </Link>
                    <Link
                      to="/grade-analysis/nonsul"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1 pl-6 text-sm",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      📝 논술 전형
                    </Link>
                  </div>
                  )}

                  <div className="space-y-6">
                    <div className="w-full space-y-4">
                      <h3 className="mb-2 font-semibold">수시서비스</h3>
                      <div className="flex flex-col gap-1 text-sm">
                        <Link to="/susi" onClick={() => setIsOpen(false)} className="py-1">
                          💁 사용안내
                        </Link>
                        <Link to="/susi/school-record" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">1</span>
                          📄 생기부 입력
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/performance" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">2</span>
                          📊 성적 분석
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/compatibility" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">3</span>
                          🌏 계열 적합성 진단
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/request" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">4</span>
                          🧑‍🏫 평가 신청(AI/사정관)
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/evaluation-list" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">5</span>
                          📋 생기부 평가 내역
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/subject" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">6</span>
                          📚 교과 전형 탐색
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/comprehensive" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">7</span>
                          🧐 학종 전형 탐색
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/nonsul" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">8</span>
                          📝 논술 전형 탐색
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/interest" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">9</span>
                          🏫 관심대학
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/susi/combination" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">10</span>
                          🎯 모의지원
                        </Link>
                      </div>
                    </div>

                    <div className="w-full space-y-4">
                      <h3 className="mb-2 font-semibold">정시서비스</h3>
                      <div className="flex flex-col gap-1 text-sm">
                        <Link to="/jungsi" onClick={() => setIsOpen(false)} className="py-1">
                          💁 사용안내
                        </Link>
                        <Link to="/jungsi/score-input" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">1</span>
                          📝 성적 입력
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/score-analysis" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">2</span>
                          📊 성적분석
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/strategy" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">3</span>
                          👻 수시/정시 지원전략
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/a" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">4</span>
                          🐢 가군 컨설팅
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/b" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">5</span>
                          🐢 나군 컨설팅
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/c" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">6</span>
                          🐢 다군 컨설팅
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/gunoe" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">7</span>
                          🐢 군외 컨설팅
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/interest" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">8</span>
                          🏫 관심대학
                        </Link>
                        <IconChevronDown className="ml-2 h-4 w-4 text-muted-foreground" />
                        <Link to="/jungsi/combination" onClick={() => setIsOpen(false)} className="flex items-center gap-2 py-1">
                          <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">9</span>
                          🎯 모의지원
                        </Link>
                      </div>
                    </div>

                    {/* Members 섹션 */}
                    <div className="w-full space-y-4">
                      <h3 className="mb-2 font-semibold">Members</h3>
                      <div className="flex flex-col gap-1 text-sm">
                        {/* My Group 서브메뉴 */}
                        <div className="font-medium text-orange-600 py-1">👥 My Group</div>
                        <Link to="/members/my-group" onClick={() => setIsOpen(false)} className="py-1 pl-4">
                          🏠 Home
                        </Link>
                        <Link to="/members/my-group/study-ranking" onClick={() => setIsOpen(false)} className="py-1 pl-4">
                          📊 학습랭킹
                        </Link>
                        <Link to="/members/my-group/quiz-ranking" onClick={() => setIsOpen(false)} className="py-1 pl-4">
                          📝 퀴즈 랭킹
                        </Link>
                        <Link to="/members/my-group/grade-ranking" onClick={() => setIsOpen(false)} className="py-1 pl-4">
                          📊 내신 랭킹
                        </Link>
                        <Link to="/members/my-group/mock-ranking" onClick={() => setIsOpen(false)} className="py-1 pl-4">
                          📈 모의고사 랭킹
                        </Link>
                        {/* 다른 Members 메뉴 */}
                        <Link to="/members/study-group" onClick={() => setIsOpen(false)} className="py-1 mt-2">
                          <span>📖 Study Group</span>
                        </Link>
                        <Link to="/members/mentoring" onClick={() => setIsOpen(false)} className="py-1">
                          <span>🎓 Mentoring</span>
                        </Link>
                        <Link to="/members/clinic" onClick={() => setIsOpen(false)} className="py-1">
                          <span>🏥 Clinic</span>
                        </Link>
                      </div>
                    </div>

                    <div className="w-full space-y-4">
                      <h3 className="mb-2 font-semibold">고객센터</h3>
                      <div className="grid grid-cols-2 gap-2 gap-y-6 text-sm">
                        <Link
                          to="/official/notice"
                          onClick={() => setIsOpen(false)}
                        >
                          공지사항
                        </Link>
                        <Link
                          to="/official/guide"
                          onClick={() => setIsOpen(false)}
                        >
                          서비스 소개
                        </Link>
                        <Link
                          to="/official/faq"
                          onClick={() => setIsOpen(false)}
                        >
                          FAQ
                        </Link>
                      </div>
                    </div>
                  </div>

                  <Separator className="my-2" />
                  <div className="w-full space-y-2">
                    <Link
                      to="/products"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      🔥 이용권 구매
                    </Link>
                    <Link
                      to="/account-linkage"
                      className={cn(
                        buttonVariants({ variant: "ghost" }),
                        "w-full justify-start px-1",
                      )}
                      onClick={() => setIsOpen(false)}
                    >
                      🔗 계정연동
                    </Link>
                    {user ? (
                      <>
                        <Link
                          to="/users/profile"
                          className={cn(
                            buttonVariants({ variant: "ghost" }),
                            "w-full justify-start px-1",
                          )}
                          onClick={() => setIsOpen(false)}
                        >
                          마이 페이지
                        </Link>
                        <Link
                          to="/users/school-record"
                          className={cn(
                            buttonVariants({ variant: "ghost" }),
                            "w-full justify-start px-1",
                          )}
                          onClick={() => setIsOpen(false)}
                        >
                          생기부/성적 입력
                        </Link>
                        <Link
                          to="/users/mock-exam"
                          className={cn(
                            buttonVariants({ variant: "ghost" }),
                            "w-full justify-start px-1",
                          )}
                          onClick={() => setIsOpen(false)}
                        >
                          모의고사/수능 성적 입력
                        </Link>
                        {isOfficer && (
                          <Link
                            to="/officer/apply"
                            className={cn(
                              buttonVariants({ variant: "ghost" }),
                              "w-full justify-start px-1",
                            )}
                            onClick={() => setIsOpen(false)}
                          >
                            평가자 전용 페이지
                          </Link>
                        )}
                        <Button
                          variant="ghost"
                          onClick={() => {
                            handleLogoutClick();
                            setIsOpen(false);
                          }}
                          className={cn(
                            "w-full justify-start px-1 text-red-500 hover:text-red-600",
                          )}
                        >
                          로그아웃
                        </Button>
                      </>
                    ) : (
                      <Link
                        to="/auth/login"
                        className={cn(
                          buttonVariants({ variant: "default" }),
                          "w-full",
                        )}
                        onClick={() => setIsOpen(false)}
                      >
                        로그인
                      </Link>
                    )}
                  </div>
                </nav>
              </SheetContent>
            </Sheet>
          </span>

          {/* 데스크탑 메뉴 */}
          <div className="hidden items-center gap-12 lg:flex xl:gap-20">
            <NavigationMenu>
              <NavigationMenuList>
                <NavigationMenuItem>
                  <NavigationMenuLink
                    className={navigationMenuTriggerStyle()}
                    asChild
                  >
                    <Link to="/">홈</Link>
                  </NavigationMenuLink>
                </NavigationMenuItem>

                {/* 플래너 메뉴 - 고3 제외 */}
                {grade !== "3" && (
                <NavigationMenuItem>
                  <NavigationMenuTrigger
                    onPointerMove={(e: any) => e.preventDefault()}
                    onPointerLeave={(e: any) => e.preventDefault()}
                  >
                    플래너
                  </NavigationMenuTrigger>
                  <NavigationMenuContent className="left-auto right-0">
                    <div className="p-4 md:w-[600px]">
                      <div className="flex flex-wrap items-center justify-center gap-1">
                        <Link to="/planner" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🏠 플래너 홈</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/planner/plans" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📋 장기 계획</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/planner/routine" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🔄 주간 루틴</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/planner/today" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">✅ 금일 할일</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/planner/learning" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📚 학습현황</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/planner/class" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">👨‍🏫 수업현황</span>
                        </Link>
                      </div>
                    </div>
                  </NavigationMenuContent>
                </NavigationMenuItem>
                )}

                {/* 내신성적분석 메뉴 - 고3, N수생 제외 */}
                {grade !== "3" && grade !== "2N" && (
                <NavigationMenuItem>
                  <NavigationMenuTrigger
                    onPointerMove={(e: any) => e.preventDefault()}
                    onPointerLeave={(e: any) => e.preventDefault()}
                  >
                    내신성적분석
                  </NavigationMenuTrigger>
                  <NavigationMenuContent>
                    <div className="p-4 md:w-[750px] lg:w-[900px]">
                      {/* 1단계 흐름 */}
                      <div className="mb-3 flex flex-wrap items-center justify-center gap-1">
                        <Link to="/grade-analysis/school-record" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📄 생기부 입력</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/grade-analysis/performance" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📊 성적 분석</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/grade-analysis/request" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🧑‍🏫 평가 신청</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/grade-analysis/evaluation-list" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📋 평가 내역</span>
                        </Link>
                      </div>

                      {/* 2단계 흐름 */}
                      <div className="flex flex-wrap items-center justify-center gap-1">
                        <Link to="/grade-analysis/subject" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📚 교과 전형</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/grade-analysis/comprehensive" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🧐 학종 전형</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/grade-analysis/nonsul" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📝 논술 전형</span>
                        </Link>
                      </div>
                    </div>
                  </NavigationMenuContent>
                </NavigationMenuItem>
                )}

{grade !== "3" && (
                <NavigationMenuItem>
                  <NavigationMenuTrigger
                    onPointerMove={(e: any) => e.preventDefault()}
                    onPointerLeave={(e: any) => e.preventDefault()}
                  >
                    모의고사분석
                  </NavigationMenuTrigger>
                  <NavigationMenuContent className="left-auto right-0">
                    <div className="p-4 md:w-[650px]">
                      <div className="flex flex-wrap items-center justify-center gap-1">
                        <Link to="/mock-analysis/score-input" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📝 성적입력</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/mock-analysis/score-analysis" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📊 성적분석</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/mock-analysis/prediction" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🏫 대학예측</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/mock-analysis/statistics" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📈 누적분석</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/mock-analysis/wrong-answers" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📝 분석과오답</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/mock-analysis/target-university" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🎯 목표대학</span>
                        </Link>
                      </div>
                    </div>
                  </NavigationMenuContent>
                </NavigationMenuItem>
                )}
{grade !== "3" && (
                <NavigationMenuItem>
                  <NavigationMenuTrigger
                    onPointerMove={(e: any) => e.preventDefault()}
                    onPointerLeave={(e: any) => e.preventDefault()}
                  >
                    수시서비스
                  </NavigationMenuTrigger>
                  <NavigationMenuContent>
                    <div className="p-4 md:w-[750px] lg:w-[900px]">
                      {/* 사용안내 링크 */}
                      <Link
                        to="/susi"
                        className="mb-4 block rounded-lg bg-gray-50 p-3 text-center hover:bg-gray-100"
                      >
                        <span className="text-sm font-medium">💁 사용안내</span>
                        <span className="ml-2 text-xs text-muted-foreground">- 자세한 사용방법과 QNA 확인</span>
                      </Link>

                      {/* 1단계 흐름 */}
                      <div className="mb-3 flex flex-wrap items-center justify-center gap-1">
                        <Link to="/susi/school-record" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📄 생기부 입력</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/performance" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📊 성적 분석</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/compatibility" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🌏 계열 적합성</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/request" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🧑‍🏫 평가 신청</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/evaluation-list" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📋 평가 내역</span>
                        </Link>
                      </div>

                      {/* 2단계 흐름 */}
                      <div className="flex flex-wrap items-center justify-center gap-1">
                        <Link to="/susi/subject" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📚 교과 전형</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/comprehensive" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🧐 학종 전형</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/nonsul" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📝 논술 전형</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/interest" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🏫 관심대학</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/susi/combination" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🎯 모의지원</span>
                        </Link>
                      </div>
                    </div>
                  </NavigationMenuContent>
                </NavigationMenuItem>
                )}
                <NavigationMenuItem>
                  <NavigationMenuTrigger
                    onPointerMove={(e: any) => e.preventDefault()}
                    onPointerLeave={(e: any) => e.preventDefault()}
                  >
                    정시서비스
                  </NavigationMenuTrigger>
                  <NavigationMenuContent>
                    <div className="p-4 md:w-[700px] lg:w-[850px]">
                      {/* 사용안내 링크 */}
                      <Link
                        to="/jungsi"
                        className="mb-4 block rounded-lg bg-gray-50 p-3 text-center hover:bg-gray-100"
                      >
                        <span className="text-sm font-medium">💁 사용안내</span>
                        <span className="ml-2 text-xs text-muted-foreground">- 자세한 사용방법과 QNA 확인</span>
                      </Link>

                      {/* 1단계 흐름: 성적입력 → 성적분석 → 지원전략 → 가군 → 나군 */}
                      <div className="mb-3 flex flex-wrap items-center justify-center gap-1">
                        <Link to="/jungsi/score-input" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📝 성적입력</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/score-analysis" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📊 성적분석</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/strategy" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">👻 지원전략</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/a" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🐢 가군</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/b" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🐢 나군</span>
                        </Link>
                      </div>

                      {/* 2단계 흐름: 다군 → 군외 → 관심대학 → 모의지원 */}
                      <div className="flex flex-wrap items-center justify-center gap-1">
                        <Link to="/jungsi/c" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🐢 다군</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/gunoe" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🐢 군외</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/interest" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🏫 관심대학</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/jungsi/combination" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🎯 모의지원</span>
                        </Link>
                      </div>
                    </div>
                  </NavigationMenuContent>
                </NavigationMenuItem>

                {/* Members 메뉴 - 고3 제외 */}
                {grade !== "3" && (
                <NavigationMenuItem>
                  <NavigationMenuTrigger
                    onPointerMove={(e: any) => e.preventDefault()}
                    onPointerLeave={(e: any) => e.preventDefault()}
                  >
                    Members
                  </NavigationMenuTrigger>
                  <NavigationMenuContent className="left-auto right-0">
                    <div className="p-4 md:w-[600px]">
                      {/* My Group 섹션 */}
                      <div className="mb-2 text-center">
                        <span className="text-sm font-semibold text-orange-600">👥 My Group</span>
                      </div>

                      {/* My Group 메뉴 1단계 */}
                      <div className="mb-3 flex flex-wrap items-center justify-center gap-1">
                        <Link to="/members/my-group" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">🏠 Home</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/members/my-group/study-ranking" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📊 학습랭킹</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/members/my-group/quiz-ranking" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📝 퀴즈 랭킹</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/members/my-group/grade-ranking" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📊 내신 랭킹</span>
                        </Link>
                        <IconChevronDown className="h-4 w-4 -rotate-90 text-muted-foreground" />
                        <Link to="/members/my-group/mock-ranking" className="flex items-center gap-1 rounded-lg px-3 py-2 hover:bg-accent">
                          <span className="text-sm font-medium">📈 모의고사 랭킹</span>
                        </Link>
                      </div>

                      {/* 다른 Members 메뉴 */}
                      <div className="border-t border-gray-200 pt-3">
                        <div className="flex flex-wrap items-center justify-center gap-2">
                          <Link to="/members/study-group" className="flex items-center rounded-lg px-3 py-2 hover:bg-accent">
                            <span className="text-sm font-medium">📖 Study Group</span>
                          </Link>
                          <Link to="/members/mentoring" className="flex items-center rounded-lg px-3 py-2 hover:bg-accent">
                            <span className="text-sm font-medium">🎓 Mentoring</span>
                          </Link>
                          <Link to="/members/clinic" className="flex items-center rounded-lg px-3 py-2 hover:bg-accent">
                            <span className="text-sm font-medium">🏥 Clinic</span>
                          </Link>
                        </div>
                      </div>
                    </div>
                  </NavigationMenuContent>
                </NavigationMenuItem>
                )}

              </NavigationMenuList>
            </NavigationMenu>
            <div className="flex items-center gap-2">
              <Popover>
                <PopoverTrigger asChild>
                  <Button variant={"ghost"}>
                    <span>고객센터</span>
                    <IconChevronDown className="size-4" />
                  </Button>
                </PopoverTrigger>
                <PopoverContent className="w-48 space-y-1 p-1">
                  <Link
                    to="/official/notice"
                    className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                  >
                    공지사항
                  </Link>
                  <Link
                    to="/official/guide"
                    className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                  >
                    서비스 소개
                  </Link>
                  <Link
                    to="/official/faq"
                    className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                  >
                    FAQ
                  </Link>
                </PopoverContent>
              </Popover>
              <Link
                to="/products"
                className={cn(
                  buttonVariants({ variant: "ghost" }),
                  "px-3 py-1 text-sm",
                )}
              >
                🔥 이용권 구매
              </Link>
              <Link
                to="/account-linkage"
                className={cn(
                  buttonVariants({ variant: "ghost" }),
                  "px-3 py-1 text-sm",
                )}
              >
                🔗 계정연동
              </Link>

              {user ? (
                <Popover>
                  <PopoverTrigger asChild>
                    <Button variant={"ghost"}>
                      <span>{user.nickname} 님</span>{" "}
                      <IconChevronDown className="size-4" />
                    </Button>
                  </PopoverTrigger>
                  <PopoverContent className="w-48 space-y-1 p-1">
                    <Link
                      to="/users/profile"
                      className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                    >
                      마이 페이지
                    </Link>
                    <Link
                      to="/users/school-record"
                      className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                    >
                      생기부/성적 입력
                    </Link>
                    <Link
                      to="/users/mock-exam"
                      className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                    >
                      모의고사/수능 성적 입력
                    </Link>
                    <Link
                      to="/users/payment"
                      className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                    >
                      결제내역
                    </Link>
                    {isOfficer && (
                      <Link
                        to="/officer/apply"
                        className="flex h-8 w-full items-center rounded-md px-2 text-sm hover:bg-gray-100"
                      >
                        평가자 전용 페이지
                      </Link>
                    )}
                    <Separator />

                    <Dialog>
                      <DialogTrigger asChild>
                        <Button
                          variant={"ghost"}
                          className="flex h-8 w-full items-center justify-start rounded-md px-2 text-sm font-normal text-red-500 hover:bg-gray-100 hover:text-red-500"
                        >
                          로그아웃
                        </Button>
                      </DialogTrigger>
                      <DialogContent className="w-full max-w-[300px]">
                        <DialogHeader>
                          <DialogTitle>로그아웃 하시겠습니까?</DialogTitle>
                        </DialogHeader>
                        <DialogFooter className="gap-4">
                          <DialogClose>취소</DialogClose>
                          <Button onClick={handleLogoutClick}>확인</Button>
                        </DialogFooter>
                      </DialogContent>
                    </Dialog>
                  </PopoverContent>
                </Popover>
              ) : (
                <Link to="/auth/login" className={cn(buttonVariants())}>
                  로그인
                </Link>
              )}
            </div>
          </div>
        </div>
      </div>
    </header>
    </>
  );
};


const ListItem = ({
  className,
  title,
  children,
  href,
  disabled,
  step,
}: {
  className?: string;
  title: string;
  children?: React.ReactNode;
  disabled?: boolean;
  href: string;
  step?: number;
}) => {
  return (
    <li>
      <NavigationMenuLink asChild>
        <Link
          to={href}
          disabled={disabled}
          className={cn(
            "block select-none space-y-1 rounded-md p-3 leading-none no-underline outline-none transition-colors hover:bg-accent hover:text-accent-foreground focus:bg-accent focus:text-accent-foreground",
            className,
          )}
        >
          <div className="flex items-center gap-2 text-sm font-medium leading-none">
            {step && (
              <span className="inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary text-xs text-white">
                {step}
              </span>
            )}
            {title}
          </div>
          <p className={cn("line-clamp-2 text-sm leading-snug text-muted-foreground", step && "ml-7")}>
            {children}
          </p>
        </Link>
      </NavigationMenuLink>
    </li>
  );
};
ListItem.displayName = "ListItem";
