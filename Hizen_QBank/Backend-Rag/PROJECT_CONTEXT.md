# 📊 Project Context Map

## Project Overview
**Name**: google
**Version**: 1.0.0
**Type**: Node.js CommonJS
**Purpose**: Gemini API File Search Example

---

## 🏗️ Project Structure
```
GoogleFileSearch/
├── FileSearchManager.js    [Core library]
├── example.js              [Usage example]
├── package.json            [Dependencies & config]
├── package-lock.json       [Dependency lock]
└── node_modules/           [Dependencies installed]
```

---

## 📦 Dependencies
- **@google/genai** (^1.29.1) - Google Gemini API SDK

---

## 🔧 Environment
- **Node.js**: v22.15.1
- **npm**: 10.9.2
- **Module System**: CommonJS

---

## 🎯 Core Components

### **FileSearchManager.js** (FileSearchManager.js:6)
Gemini API File Search 기능 추상화 클래스

**Key Methods**:
- `createStore(displayName)` - Store 생성 (FileSearchManager.js:22)
- `listStores(pageSize)` - Store 목록 조회 (FileSearchManager.js:34)
- `deleteStore(storeName, force)` - Store 삭제 (FileSearchManager.js:50)
- `uploadFile(filePath, storeName, options)` - 파일 업로드 (FileSearchManager.js:66)
- `listDocuments(storeName)` - 문서 목록 (FileSearchManager.js:95)
- `deleteDocument(documentName, force)` - 문서 삭제 (FileSearchManager.js:111)
- `search(query, storeNames, model)` - File Search 검색 (FileSearchManager.js:125)
- `getStoreInfo(storeName)` - Store 정보 조회 (FileSearchManager.js:149)

---

## ⚠️ Security Notes
**API Key Exposure**: example.js:2에 API 키가 하드코딩되어 있음
- **Risk**: 공개 저장소에 노출 시 보안 위험
- **Recommendation**: 환경 변수 사용 (process.env.API_KEY)

---

## 📝 Usage Pattern
```javascript
const manager = new FileSearchManager(API_KEY);
const store = await manager.createStore('name');
await manager.uploadFile('file.pdf', store.name);
const result = await manager.search('query', store.name);
```

---

## 🚀 Next Steps Suggestions
1. **Security**: API 키를 .env 파일로 분리
2. **Testing**: 테스트 스크립트 추가 (현재 없음)
3. **Documentation**: README.md 작성
4. **Type Safety**: TypeScript 마이그레이션 고려
5. **Error Handling**: 상세한 에러 처리 추가

---

*Generated by Claude Code SuperClaude Framework - /sc:load command*
